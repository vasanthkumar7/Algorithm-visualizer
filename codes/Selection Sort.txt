def sel_sort(a):

    for i in range(len(a)-1):
        mins=i
        for j in range(i+1,len(a)):
            if a[j]<a[mins]:
                mins=j
                
        if mins!=i:
            temp=a[mins]
            a[mins]=a[i]
            a[i]=temp
       